@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor Accessor
<div >
    <h1 >Bienvenido</h1>
    <p>agua</p>
         @{//Name: @HttpContext.Session.GetString("hola")
    // var fff = _httpContextAccessor?.HttpContext?.Session.GetString("SessionVar");
      var rr = Accessor.HttpContext?.Session.GetString("hola");
     // HttpContext.Session.GetString()
     }
    @{WriteLiteral(DKbase.Helper.getTipoApp);}
    <br/>
        @{WriteLiteral(DKweb.Controllers.mvcController.isPrueba(Accessor));}
</div>
